function removeFromArray(arr,minus) { // minus = 3
  const temp = [];

  for (let i = 0; i < arr.length; i++) {
    if (arr[i] !== minus) {
      temp.push(arr[i]);
    }
  }
  return temp;
}

module.exports = removeFromArray;


/* const removeFromArray = function (arr, minus) {
    var arr = [1, 2, 3, 4];
    var minus = 0 ;
    
    for (let i = 0; i < arr.length; i++ ) {
         if 
        return ;
}

}; */
    

/* 
    function removeFromArray(arr, minus) {
  const temp = [];

  for (let j = 0; j < arr.length; j++) {
    console.log('hi')
  }

  return arr;
}

*/













 
 
 /* const removeFromArray = function (arr, minus) {
    var arr = [1, 2, 3, 4]
    var minus = 1; 
    /* var minus = arr.indexOf(3);
        if (minus > -1) {
            arr.splice(minus, 1)
        } */


     

// Do not edit below this line
// module.exports = removeFromArray;

// .push(), .unshift() .pop()
// remove the middle number 

/* removeFromArray([1, 2, 3, 4], 3); // should remove 3 and return [1,2,4] */




/* console.log(reverse('my name is george'))

function removeFromArray(arr, minus) {
    const temp = [];

    for (let j = 0; j < arr.length; j++) {

        return arr;
    }

    const test = [ 1, 2, 3, 4, 5 ]


*/

/* function removeFromArray(arr, minus) {
    const temp = [];
  
    for (let j = 0; j < arr.length; j++) {
      console.log('hi')
    }
  
    return arr;
  } */